using ApplicationData.Data;

namespace LinQ;

public static class HandsOn
{
    public static ApplicationDbContext _db = new();
    /*
     * Hands on
     *   Get the first 3 products sorted by price in descending order
     *   Get the total stock value (UnitPrice * UnitsInStock) for each category
     *   Find the most expensive product in each category
     */
    public static void Answer()
    {
        
    }
}